 <section className="max-w-[1440px] mx-auto px-4 sm:px-6 lg:px-8 py-10 pb-16">
          <div className="flex items-center justify-between gap-4 mb-6">
            <h2 className="text-2xl md:text-3xl font-primary font-bold text-primary">
              My Favorites
            </h2>
            <span className="text-sm font-medium text-gray-500 font-primary">
              {favoriteBooks.length} saved book{favoriteBooks.length !== 1 ? "s" : ""}
            </span>
          </div>

          {favoritesLoading ? (
            <div className="rounded-2xl border border-gray-200 bg-white p-8 text-center text-gray-500 font-primary">
              Loading favorite books...
            </div>
          ) : favoritesError ? (
            <div className="rounded-2xl border border-red-200 bg-red-50 p-8 text-center text-red-600 font-primary">
              {favoritesError}
            </div>
          ) : favoriteBooks.length === 0 ? (
            <div className="rounded-2xl border border-gray-200 bg-white p-8 text-center text-gray-500 font-primary">
              You have not saved any books yet.
            </div>
          ) : (
            <div className="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
              {favoriteBooks.map((book) => (
                <Link
                  key={book.id}
                  to={`/bookDetail/${book.id}`}
                  className="group overflow-hidden rounded-2xl border border-gray-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-lg"
                >
                  <div className="aspect-[16/10] overflow-hidden bg-gray-100">
                    <img
                      src={book.cover_image}
                      alt={book.title}
                      className="h-full w-full object-cover transition duration-300 group-hover:scale-105"
                    />
                  </div>
                  <div className="p-4">
                    <p className="text-lg font-primary font-bold text-primary line-clamp-1">
                      {book.title}
                    </p>
                    <p className="text-sm font-primary text-gray-500 line-clamp-1">
                      {book.author}
                    </p>
                  </div>
                </Link>
              ))}
            </div>
          )}
        </section>